---
title: '백준 온라인 저지에 잇창명이 출제한 문제 모음'
published: 2024-02-28
edited: 2026-04-30
tags:
- '문제해결'
- '알고리즘'
- '회고'
---

import { Image } from 'astro:assets';

import AOut from '@components/post/a/AOut.astro';
import ABoj from '@components/post/a/ABoj.astro';
import AWp from '@components/post/a/AWp.astro';
import AYt from '@components/post/a/AYt.astro';
import Caution from '@components/post/Caution.astro';
import Dive from '@components/post/Dive.astro';
import Blockquote from '@components/post/Blockquote.astro';
import WA from '@components/post/boj/WA.astro';
import Statement from '@components/post/boj/Statement.astro';
import SolvedTag from '@components/post/boj/Tag.astro';
import SolvedTier from '@components/post/boj/Tier.astro';
import Term from '@components/post/Term.astro';
import Fn from '@components/post/fn/Fn.astro';
import Tn from '@components/post/fn/Tn.astro';

import * as Table from './Table.tsx';

import cpc2024f1_aejuga from './cpc2024f1-aejuga.png';
import x1829849604579606873 from './1829849604579606873.x.png';

그동안 중앙대학교 알고리즘 학회 ChAOS에 소속되어 백준 온라인 저지에 대회 문제를 출제해보는 귀중한 기회를 몇 번 가졌었는데, 생각해보니 대회 공식 에디토리얼 이외에 블로그에 문제에 대한 이야기를 한 적이 없었네요. 마침 생각난 김에 그동안 출제했던 문제의 출제 의도를 하나씩 풀어보려고 합니다. (정해 코드는 없습니다!)

~~글 작성 이후에도 <AOut href="https://www.acmicpc.net/user/dlaud5379" unavailable>`dlaud5379`</AOut> 명의로 다른 문제를 출제할 때마다 업데이트할 예정입니다.~~

<Caution>
백준 온라인 저지는 2026년 4월 28일부로 서비스를 종료했으며, 5월 29일 18시부터 읽기 전용 아카이브로 다시 접속 가능할 예정입니다. 해당 날짜 이전까지는 이 글에서 지문을 확인해 주시기 바랍니다. CPC 2024와 CPC 2026의 에디토리얼은 정상적으로 확인 가능합니다.

여건이 된다면 다른 온라인 저지로 해당 문제를 옮길 의향이 있지만, 4월 30일 현재 정답 코드를 제출해볼 수 있는 서비스는 아직 없습니다.
</Caution>

## CPC 2020 \{#cpc2020\}

<AOut href="https://www.acmicpc.net/category/detail/2345" unavailable>2020 중앙대학교 프로그래밍 경진대회</AOut>에 한 문제를 출제하고, 두 문제에 삽화를 제공했습니다.

### <ABoj prob={20210} /> (F번) \{#cpc2020f\}

<Statement prob={20210} />

서비스 종료 이전 이 문제의 난이도는 <SolvedTier long tier="g3" />이며, 알고리즘 분류는 <SolvedTag tag="implementation" /> <SolvedTag tag="string" /> <SolvedTag tag="sorting" />이었습니다. 출제 이후 거의 4년이 지난 지금(2024년 2월 8일) 구글에 `백준 20210`으로 검색해 봤더니 생각보다 이 문제에 대한 블로그 글이 많네요. 풀어주셔서 감사합니다 🙇

지문에서 짐작할 수 있듯이 표준 라이브러리에서 제공하는 정렬 함수에 비교 함수를 만들어 호출하면 되는 정직한 문제입니다. 물론 그 비교 함수 구현이 좀 복잡하긴 합니다. 비교 함수의 조건을 간단하게 요약하면 이렇습니다.

* 숫자는 무조건 문자보다 작다.
* 문자끼리 비교
	* 대소문자 구분 없이 사전순으로 비교하고,
	* 같은 문자라면 대문자가 소문자보다 작다.
* 숫자끼리 비교
	* 최대한 긴 숫자열로 묶는다.
	* 십진법으로 읽은 값으로 비교하고,
	* 같은 값이라면 불필요한 0이 적을수록 작다.

<AOut href="https://www.acmicpc.net/source/23980193" unavailable>제가 의도한 C++14 풀이</AOut>에서 비교 함수의 전체적인 구조는 이렇습니다. 입력 문자열의 길이 n에 대해 비교 함수의 시간 복잡도는 $$O(n)$$입니다.

```
// C++ 스타일 의사코드입니다.

// 좌변이 작으면 음수, 우변이 작으면 양수, 같으면 0
int 문자끼리_비교(char 좌변, char 우변) {
	if(대문자로 변환한 좌·우변이 다름)
		return (대문자로 변환한 좌변이 다르면 -1, 아니면 1);
	return (좌·우변의 대문자 여부만 추출해서 비교);
}

// 좌변이 우변 미만이어야 `true`
bool 비교함수(const string &좌변, const string &우변) {
	while(좌·우변을 첫 글자부터 스캔하면서) {
		if(우변이 일찍 끝남)
			return false;
		if(좌변이 일찍 끝남)
			return true;

		if(좌·우변의 숫자/문자 여부가 다름)
			return (좌변이 숫자인지 여부);
		if(좌·우변이 문자) {
			// 문자끼리 비교
			int 비교결과 = 문자끼리_비교(현재 좌·우변에서 스캔하는 글자);
			if(비교결과 != 0)
				return 비교결과 < 0;
		} else {
			// 숫자열끼리 비교

			// 값으로 비교
			if(좌·우변에서 불필요한 0을 뺀 길이가 다름)
				return (불필요한 0을 뺀 좌·우변의 문자열 길이끼리 비교);
			int 비교결과 = (불필요한 0을 뺀 좌·우변을 사전식으로 비교);
			if(비교결과 != 0)
				return 비교결과 < 0;
			// 불필요한 0의 개수로 비교
			if(좌·우변의 불필요한 0의 개수가 다름)
				return (좌·우변의 불필요한 0의 개수로 비교);
			// 두 숫자열이 일치함
		}
	}
}
```

## CHAC 2022 \{#chac2022\}

<AOut href="https://www.acmicpc.net/category/detail/3016" unavailable>ChAOS Hello2022 Algorithm Contest</AOut>에 두 문제를 출제하고, 세 문제에 삽화를 제공했습니다.

CPC 2020에 문제를 출제할 때 지문에 '잇창명'을 넣지 않은 게 후회되어서 이번에 출제하는 두 문제에는 모두 '잇창명'을 넣었습니다.

### <ABoj prob={24390} /> (D번) \{#chac2022d\}

<Statement prob={24390} />

서비스 종료 이전 이 문제의 난이도는 <SolvedTier long tier="s2" />이며, 알고리즘 분류는 <SolvedTag tag="graphs" /> <SolvedTag tag="greedy" /> <SolvedTag tag="graph_traversal" /> <SolvedTag tag="bfs" />이었습니다.

처음 출제했을 때는 <SolvedTag tag="greedy" />과 <SolvedTag tag="bfs" />만을 정해로 생각했는데, 문제 검수 중에 <SolvedTag tag="dp" />으로 풀 수 있다는 것을 알게 되어 결과적으로는 **정해가 사실상 3개**인 문제가 되었습니다. 개인적으로 좋은 문제가 되었다고 생각해 꽤 만족스럽네요.

#### 너비 우선 탐색 \{#chac2022d-bfs\}

가장 직관적으로 떠오를 만한 풀이입니다. 조리시간(`int`)과 조리 여부(`bool`)의 순서쌍을 정점으로, 각 버튼마다 가능한 상태 변화를 유향 간선으로 삼고 $$(0, \mathrm{false})$$ 노드에서 출발하는 너비 우선 탐색을 할 수 있습니다.

#### 그리디 알고리즘 \{#chac2022d-greedy\}

<AOut href="https://www.acmicpc.net/source/38169595" unavailable>제가 처음 의도했던 C11 풀이이기도 합니다</AOut>. 당시 <AOut href="https://gather.town/">Gather</AOut>에서 공개했던 에디토리얼에 그리디 알고리즘에 대한 증명을 실었었는데, 대중에 공개된 링크를 못 찾겠네요. 이 블로그에는 좀 더 엄밀하게 다듬은 증명을 싣겠습니다.

30초 버튼의 특수한 성질을 무시하고 문제를 풀면 30초 버튼을 정확히 0번 혹은 1번 누를 수 있습니다(30초 다음 단위가 60초이므로).

* **30초 버튼을 1번 누르는 경우**: 30초 버튼을 가장 처음에 누르면 정상적으로 30초가 추가되*면서* 조리가 시작되므로 일반적인 탐욕법과 같은 해를 가집니다.
* **30초 버튼을 누르지 않는 경우**: 이때는 조리가 시작되지 않으므로 어떻게든 30초 버튼을 추가로 눌러야 합니다. 편의상 추가로 버튼을 누르는 횟수를 $$n$$이라고 하겠습니다.
	* 모든 경우에 대해 $$n = 1$$인 알고리즘이 존재합니다.
		* 모든 버튼을 누르고 나서 **30초 버튼을 마지막에 추가로 누를** 수 있는데, 아직 조리가 시작되지 않았으므로 조리가 시작되고 원래 조리시간이 0이 아니었으므로 조리시간은 유지됩니다.
	* 탐욕법의 성질에 의해 $$n \le 0$$일 수는 없습니다.
	* 위의 논리에 의해 일반적인 탐욕법으로 구한 해에 $$n = 1$$을 더한 것이 해가 됩니다.

추가로 입력 범위가 작은 것을 이용해 너비 우선 탐색 코드와 탐욕법 코드가 모든 입력에 대해 같은 출력을 하는지 체크하는, 말 그대로 'proof by AC'<Fn id="proof-by-ac" />를 했습니다.

#### 동적 계획법 \{#chac2022d-dp\}

위의 너비 우선 탐색 풀이를 그대로 동적 계획법으로 바꿀 수 있습니다. 조리시간 `i`와 조리 여부 `j`에 대해 `dp[i][j]`를 만들고 풀면 됩니다.

### <ABoj prob={24394} /> (G번) \{#chac2022g\}

<Statement prob={24394} />

서비스 종료 이전 이 문제의 난이도는 <SolvedTier long tier="g2" />이며, 알고리즘 분류는 <SolvedTag tag="math" />이었습니다. 원래 $$O(T)$$를 의도한 문제라서 난이도 의견에 "$$O(TN)$$이 [출제자의] 의도"라는 말을 보고 깜짝 놀랐습니다. (solved.ac 가이드라인인 '가장 쉽게 풀었을 때' 기준으로는 $$O(TN)$$이 맞긴 합니다.)

#### 소재와의 연관성 \{#chac2022g-motivation\}

제가 남긴 solved.ac 난이도 의견에서 볼 수 있듯이 이 문제의 소재는 실존하는 리듬 게임인 <AOut href="https://arcaea.lowiro.com/">Arcaea</AOut>, 특히 점수 체계입니다. 특히 문제에서 제시한 판정 체계는 실제 게임의 판정 체계에서 용어만 더욱 대중적인 것으로 바꾼 것입니다.

* Perfect = Shiny PURE
* Great = Early/Late PURE
	* 인게임에서는 기본적으로 세 판정이 모두 PURE로만 표시되며, Early/Late PURE는 따로 표시하도록 하는 숙련자용 설정이 있습니다.
* Good = FAR
* Miss = LOST

이 게임을 다루는 비공식 위키인 Arcaea Wiki의 <AOut href="https://bw.artemislena.eu/arcaea/wiki/Scoring">Scoring</AOut><Fn id="breezewiki" /> 문서에서 실제 게임의 점수 체계를 확인해볼 수 있는데, 이 역시 기준 점수(10'000'000점)를 제외하고 문제에서 제시하는 것과 일치합니다.

* Shiny PURE = 100% + 1점
	* 이때 100%는 기준 점수를 노트 수로 나눈 것을 기준으로 합니다.
* Early/Late PURE = 100%
* FAR = 50%
* LOST = 0%
* 콤보는 점수에 전혀 영향이 없습니다. 문제에서도 혼동을 피하기 위해 콤보와 관련된 언급을 일절 하지 않았습니다.

원래는 문제에서도 기준 점수를 10,000,000점으로 두었지만, 범위가 너무 작아서 의도치 않은 풀이를 허용할 수 있다는 검수진의 의견을 수용해 1,000,000,000점으로 늘렸습니다.

마지막으로 <AOut href="https://bw.artemislena.eu/arcaea/wiki/Scoring#Trivia">같은 문서의 Trivia 단락</AOut>에는 다음과 같은 언급이 있습니다. 원문에 없는 강조를 추가했습니다.

<Blockquote href="https://bw.artemislena.eu/arcaea/wiki/Scoring#Trivia">
* 이론적으로는 채보<Tn id="chart" />에 충분한 수의 노트가 있을 경우 Pure Memory<Fn id="pure-memory" /> 없이도 10,000,000점을 획득하는 것이 가능하다.
	* 예를 들어, 노트가 2,500개 있는 채보에서 2,499 shiny PURE + 1 FAR를 받으면 10,000,499점이 된다.
	* 최소 $$\Bigl\lceil 0.5 \times (1 + \sqrt{(2 \times 10,000,000) + 1}) \Bigr\rceil = 2237$$개의 노트가 필요하다. **현재 최대 콤보가 2237 이상인 채보가 없으므로 이는 불가능하다.**

<Fragment slot="original">
* Theoretically, it would be possible to get over 10,000,000 points without a Pure Memory, provided the chart had enough notes.
	* For example, if a chart has 2,500 notes, 2,499 shiny PUREs + 1 FAR gives a score of 10,000,499.
	* A minimum of $$\Bigl\lceil 0.5 \times (1 + \sqrt{(2 \times 10,000,000) + 1}) \Bigr\rceil = 2237$$ notes would be necessary. **Since there are currently no charts with a max combo of 2237 or greater, this is impossible.**
</Fragment>

<Fragment slot="cite">Arcaea Wiki 중 Scoring 항목</Fragment>
</Blockquote>

이 서술에서 문제의 정해에 대한 힌트를 얻었습니다. 오래 전에 트위터에서 다른 리듬 게임인 Cytus를 소재로 목표 점수를 입력하면 그 점수를 정확히 맞추는 플레이를 출력하는 콘솔 프로그램을 봤던 (것으로 기억하는) 것도 어느 정도 영향이 있었습니다.

다 쓰고 나서 Arcaea라는 배경지식을 지우니 '그게 뭔데' 싶은 문제가 되어버렸고, 구글 검색이나 solved.ac 난이도 기여에서도 "문제의 의도를 모르겠다"는 반응이 어느 정도 보여서 정말 아쉽습니다. 노트 개수 제한을 20,000이 아니라 22,361로 뒀으면 의도가 더 드러났을까 싶네요.

#### 출제자가 의도한 풀이 \{#chac2022g-main-solution\}

먼저 출력 설명의 "가능한 (2*a*+2*b*+*c*, *a*)의 쌍이 여러 개인 입력은 없다"와 바로 위에서 언급한 "[*N*의 범위 제한을] 22,361로 뒀으면 의도가 더 드러났을까 싶네요"에 주목해야 하는데, 22,361은 위에서 인용된 2,237과 수상하게 비슷해 보이는 데서 알 수 있듯이 모든 노트를 Great 이상으로 처리해야 1,000,000,000점을 달성할 수 있는 최대 노트 수입니다. 이게 풀이랑 무슨 관련이 있냐고요?

문제에서 Good 판정을 일종의 단위로 사용한 것이 또 하나의 힌트가 되는데, 최종 점수 식을 다음과 같이 두 부분으로 분리할 수 있습니다.

* $$10^9 \times \frac{2a + 2b + c}{2N} = (2a + 2b + c)\frac{10^9}{2N}$$ (Good 단위/'큰 단위')
* $$a$$ ('작은 단위')

모든 점수를 $$a \; \mathrm{Good} + b$$로 적고 $$a$$를 특정한 값으로 고정했을 때 가능한 점수의 집합을 '띠'라고 합시다. 그러면 주어진 조건 내에서 서로 다른 어떤 띠끼리도 겹치지 않는다는 것을 알 수 있습니다.

더욱 부연 설명을 하자면, 큰 단위의 점수가 $$a$$일 때 작은 단위의 점수를 받을 수 있는 유일한 판정인 Perfect의 최대 개수는 $$\lfloor \frac{a}{2} \rfloor$$입니다. 여기서 이 띠가 가질 수 있는 작은 단위의 범위가 $$0 \le b \le \lfloor \frac{a}{2} \rfloor$$가 됨을 알 수 있습니다.

그런데 $$1 \; \mathrm{Good} = \frac{10^9}{2N}$$이므로 작은 단위의 범위가 이 값을 넘어가면 띠끼리 겹치는 상황이 생깁니다. 문제의 범위 $$N \le 20\,000$$을 대입하면 $$1 \; \mathrm{Good} = 25\,000$$, $$\lfloor \frac{a}{2} \rfloor \le 10\,000$$이므로 그런 일은 발생하지 않습니다. 그래서 이게 풀이랑 무슨 관련이 있냐고요?

이제 띠끼리 겹치지 않는다는 사실을 알았으니 점수의 작은 단위를 신경쓰지 않고 **큰 단위를 먼저 구할 수 있고**, 그 다음에 작은 단위를 구할 수 있습니다. 가능한 큰 단위는 최대 하나뿐이므로 테스트 케이스당 $$O(1)$$에 답을 구할 수 있습니다.

<AOut href="https://www.acmicpc.net/source/38169965" unavailable>제가 의도한 C11 풀이</AOut>를 보면 사칙연산만을 사용해 큰 단위와 작은 단위를 하나씩 구하고, 점수가 띠 안에 들어가는지 확인하는 로직 한 번으로 테스트 케이스가 끝나는 것을 볼 수 있습니다. 사실 식만 보면 이 코드가 왜 잘 돌아가는지 이해하기 어려울 수 있는데, 아래에 증명을 실어 보겠습니다. 정해 코드를 상수만 수정해서 <ABoj prob={35455} />의 밸리데이터로 사용하기로 했을 때 대회 디스코드에서 정당성을 증명해서 올린 적이 있는데, 이 내용을 좀 더 다듬은 것입니다.

<Dive>
<details>
<summary>증명 확인하기</summary>

**Definition.** $$\mathrm{Good}$$ ('큰 점수') = $$\frac{10^9}{2N}$$

**Definition.** '작은 점수' = $$1$$

**Definition.** Perfect, Great, Good의 개수를 각각 $$a$$, $$b$$, $$c$$로 적는다.

**Lemma 1.** 작은 점수는 항상 큰 점수의 절반 이하이다.

**Proof.** $$\mathrm{Perfect} = 2 \; \mathrm{Good} + 1$$, $$\mathrm{Great} = 2 \; \mathrm{Good}$$, $$\mathrm{Good} = 1 \; \mathrm{Good}$$이므로 큰 점수 $$x$$는 $$2a + 2b + c$$, 작은 점수 $$y$$는 $$c$$이다. 두 식을 비교하면 $$y \le \frac{x}{2}$$임을 알 수 있다. ■

**Lemma 2.** $$0 \le y \le \frac{x}{2}$$인 플레이는 항상 가능하다.

**Proof.** $$x$$가 홀수인 경우 Good 하나를 빼서 짝수로 만들고, 나머지를 Perfect와 Great에 적절히 배분하면 된다. ■

**Lemma 3 (점수 띠의 겹치지 않음 성질).** $$N \le 20\,000$$의 범위에서, $$S$$를 만드는 $$(x, y)$$의 쌍은 존재한다면 유일하다.

**Proof.** 큰 점수는 $$\mathrm{Good} \triangleq \frac{10^9}{2N}$$이고, 버림을 고려하면 $$\left\lfloor \frac{10^9}{2N} \right\rfloor$$이므로 $$y$$가 이 값을 넘지 않으면 유일성이 보장된다. $$N = 20\,000$$일 때 $$\left\lfloor \frac{10^9}{2N} \right\rfloor = 25\,000$$이므로 $$\left\lfloor \frac{10^9}{2N} \right\rfloor$$의 단조 감소 성질에 의해 $$N \le 20\,000$$의 범위 전체에 대해 성립한다. ■

**Lemma 3**에 의해 큰 점수($$x \; \mathrm{Good}$$)를 먼저 구하고 큰 점수를 이용해 작은 점수($$y$$)를 구할 수 있습니다. 여기까지가 위에서 언급한 '점수 띠가 겹치지 않음' 성질입니다. 사실 $$x$$가 $$2N$$보다 커지는 것을 막지 않으면 **Lemma 3** ($$N \le 22\,361$$ 버전)이 반증될 수 있는데, 정해 코드가 정확히 그렇게 짜여 있기 때문에 제한을 $$N \le 22\,361$$로 뒀다면 사고가 났을 수도 있습니다... 🥲

지금부터는 정수 나눗셈을 사용하는 식의 정당성을 증명하겠습니다. 구하는 $$x$$는 다음을 만족해야 합니다.

$$
\lfloor x \; \mathrm{Good} \rfloor \le S < \lfloor (x + 1) \; \mathrm{Good} \rfloor
$$

**Lemma 4 (정수에서 ≤와 \<의 관계).** 두 **정수** $$a$$, $$b$$에 대해 $$a \le b \leftrightarrow a < b + 1$$

**Proof.** ~~It's trivial.~~ ■

**Lemma 5 (\< 너머로 넘기기).** 두 실수 $$a$$, $$b$$에 대해 $$\lfloor a \rfloor < b \leftrightarrow a < \lceil b \rceil$$

**Proof.** 양쪽 방향의 증명이 비슷하므로 정방향만 증명한다.

* $$a < b$$인 경우: $$\lfloor a \rfloor \le a < b \le \lceil b \rceil$$이므로 성립한다.
* $$a \ge b$$인 경우: $$\lfloor a \rfloor < b \le a < \lfloor a \rfloor + 1$$이다. 이때 $$\lfloor \cdot \rfloor$$와 $$\lceil \cdot \rceil$$의 성질에 의해 $$\lceil b \rceil = \lfloor a \rfloor + 1$$임을 보일 수 있고, $$a < \lfloor a \rfloor + 1 = \lceil b \rceil$$이므로 성립한다.

**Lemma 6 (≤ 너머로 넘기기).** 두 실수 $$a$$, $$b$$에 대해 $$a \le \lfloor b \rfloor \leftrightarrow \lceil a \rceil \le b$$

**Proof.** ~~Left as an exercise to the reader.~~ ■

지금까지 증명한 보조정리를 활용해 부등식의 안팎을 뒤집어서 $$x$$가 안쪽으로 가도록 할 수 있습니다.

* $$\lfloor x \; \mathrm{Good} \rfloor \le S$$
	* $$\lfloor x \; \mathrm{Good} \rfloor < S + 1$$ (**Lemma 4**)
	* $$x \; \mathrm{Good} < \lceil S + 1 \rceil$$ (**Lemma 5**)
	* $$x \; \mathrm{Good} < S + 1$$ ($$S$$가 정수이므로)
	* $$x < \frac{S + 1}{\mathrm{Good}}$$ (양변을 $$1 \; \mathrm{Good}$$으로 나누기)
	* $$\lfloor x \rfloor < \frac{S + 1}{\mathrm{Good}}$$ (구하는 $$x$$가 정수이므로)
	* $$x < \left\lceil \frac{S + 1}{\mathrm{Good}} \right\rceil$$ (**Lemma 5**)
* $$S < \lfloor (x + 1) \; \mathrm{Good} \rfloor$$
	* $$S + 1 \le \lfloor (x + 1) \; \mathrm{Good} \rfloor$$ (**Lemma 4**)
	* $$\lceil S + 1 \rceil \le (x + 1) \; \mathrm{Good}$$ (**Lemma 6**)
	* $$S + 1 \le (x + 1) \; \mathrm{Good}$$ ($$S$$가 정수이므로)
	* $$S + 1 - 1 \; \mathrm{Good} \le x \; \mathrm{Good}$$ ($$1 \; \mathrm{Good}$$을 이항)
	* $$\frac{S + 1}{\mathrm{Good}} - 1 \le x$$ (양변을 $$1 \; \mathrm{Good}$$으로 나누기)
	* $$\frac{S + 1}{\mathrm{Good}} - 1 \le \lfloor x \rfloor$$ (구하는 $$x$$가 정수이므로)
	* $$\left\lceil \frac{S + 1}{\mathrm{Good}} \right\rceil - 1 \le x$$ (**Lemma 6**)

$$
\therefore \left\lceil \frac{S + 1}{\mathrm{Good}} \right\rceil - 1 \le x < \left\lceil \frac{S + 1}{\mathrm{Good}} \right\rceil
$$

상·하한이 정수이고 1만큼 차이가 나므로 하한인 $$\left\lceil \frac{S + 1}{\mathrm{Good}} \right\rceil - 1$$이 큰 점수 $$x$$가 됩니다. $$\mathrm{Good}$$의 정의를 적용하면 $$\left\lceil \frac{2N(S + 1)}{10^9} \right\rceil - 1$$이 됩니다. 참고로 정수 $$x$$와 $$y$$에 대해 $$\left\lceil \frac{x}{y} \right\rceil$$는 `(x + y - 1)/y`로 계산할 수 있습니다.

이제 작은 점수 $$y$$는 $$S - \lfloor x \; \mathrm{Good} \rfloor$$으로 구할 수 있습니다. 마지막으로 **Lemma 1**과 **Lemma 2**에 의해 불가능한 경우를 판정하면(즉, $$2y \le x$$) 문제를 해결할 수 있습니다. ■
</details>
</Dive>

아무튼 정해가 배배 꼬인 것도, 힌트가 너무 꽁꽁 숨겨져있던 것도 부정할 수는 없을 것 같습니다. 출제를 하면서 <SolvedTag tag="ad_hoc" />을 붙일지 말지 고민했는데, 지금 알고리즘 분류를 봐도 의외로 <SolvedTag tag="ad_hoc" />은 없네요.

#### 문제 여담 \{#chac2022g-trivia\}

문제에 나온 것과 같이 실제로 저도 취미로 리듬 게임을 하고(휴대폰으로는 지금도 Arcaea를 하고, 오락실에서는 사운드 볼텍스를 가장 자주 합니다), "정확히 123,456,789점을 획득해 사람들을 놀라게 하고 싶"은 ~~생각만~~ 생각도 가끔씩 합니다. **당연히 실제 잇창명은 모든 노트의 판정을 원하는 대로 조절할 수 있는 초고수 플레이어가 아닙니다.**

2026년 2월에는 원본 게임의 개발사에서 PC 리듬 게임 <AOut href="https://store.steampowered.com/app/3971950/In_Falsus/">In Falsus</AOut>의 데모를 공개했는데, 이 게임의 점수 체계도 기준 점수(100'000'000점)를 제외하고 Arcaea와 같습니다. 위의 계산식을 그대로 사용하면 노트가 $$\Bigl\lceil 0.5 \times (1 + \sqrt{(2 \times 10^8) + 1}) \Bigr\rceil = 7072$$개 이상일 때부터 띠가 겹치기 시작한다는 결과가 나옵니다.

#### 후속 문제 \{#chac2022g-sequel\}

[CPC 2026](#cpc2026)에는 이 문제를 패러디한 <ABoj prob={35455} /> (A3번) 문제가 출제되었습니다.

<em><AYt id="k3LQHbihR04">Darkest night, I'll confront you here...</AYt></em>

## CPC 2024 \{#cpc2024\}

<AOut href="https://www.acmicpc.net/category/1054" unavailable>2024 중앙대학교 프로그래밍 경진대회</AOut>에 세 문제(교내 출제 1문제, 오픈 콘테스트 전용 2문제)를 출제하고, 여섯 문제에 삽화를 제공했습니다.

<AOut href="https://upload.acmicpc.net/81489c05-5756-44b7-9205-22ada52e1d81/">공식 에디토리얼</AOut>이 공개되어 있습니다.

### <ABoj prob={32175} /> (C1번, 교내 출제) \{#cpc2024c1\}

<Statement prob={32175} />

서비스 종료 이전 이 문제의 난이도는 <SolvedTier long tier="s1" />이며, 알고리즘 분류는 <SolvedTag tag="dp" />이었습니다.

생각해보면 <SolvedTag tag="dp" />이 정해인 문제 중 점화식이 입력 데이터에 따라 동적으로 바뀌는 문제를 거의 못 본 것 같아서 기획한 문제입니다. 실제로 그런 문제가 많은데 제가 기억을 못 하는 것에 가깝지만 일단 출제 자체는 기획 의도에 맞게 잘 되긴 했습니다.

0개 이상의 컵을 쌓은 것을 '컵탑'이라고 할 때, 컵탑의 높이 $$H$$에 따라 점화식 $$B_H$$를 다음과 같이 구성할 수 있습니다.

* $$H < 0$$: 높이가 음수가 되게 쌓는 것은 당연히 불가능하므로 $$B_H = 0$$가지입니다.
* $$H = 0$$: 베이스 케이스에 해당합니다. 컵을 0개 쌓는 $$B_H = 1$$가지 방법이 있습니다.
* $$H > 0$$: 기존에 쌓은 컵탑 위에 높이가 $$A_i$$인 $$i$$번 컵을 쌓을 수 있습니다.
	* 높이가 $$A_i$$인 컵을 추가로 쌓아서 쌓인 컵탑의 높이를 $$H$$로 만들려면 쌓기 이전에 컵탑의 높이는 $$H − A_i$$여야 합니다. 즉, $$B_{H − A_i}$$가지 방법이 가능합니다.
	* 데이터에서 제시된 모든 컵에 대한 경우의 수를 합합니다.

즉, 수식으로 정리하면 다음과 같습니다.

$$
B_H = \begin{cases}
	0 & H < 0 \\
	1 & H = 0 \\
	\Sigma_{i = 1}^N B_{H - A_i} & H > 0
\end{cases}
$$

정해에서는 $$H < 0$$인 경우를 생략하고 있으며, 추가로 최근 100개 항만 메모리에 유지하는 최적화가 가능합니다($$A_i \le 100$$이므로).

### <ABoj prob={32180} /> (E1번, 오픈 콘테스트 전용) \{#cpc2024e1\}

<Statement prob={32180} />

서비스 종료 이전 이 문제의 난이도는 <SolvedTier long tier="p5" />이며, 알고리즘 분류는 <SolvedTag tag="string" /> <SolvedTag tag="parsing" /> <SolvedTag tag="recursion" />였습니다.

**이 문제만큼 난이도 예측에 실패한 적이 없을 것 같습니다.** <ABoj prob={4949} />이나 <ABoj prob={2504} /> 등의 단순 심화 버전으로 기획하고 실버 중상위권 정도의 난이도를 예상했는데, 막상 뚜껑을 열어보니 플래티넘 턱걸이를 하는 문제가 나왔습니다. 추가로 문제 자체가 검수하기 어려운 형태이고 지문도 엄밀하게 작성해야 하다 보니 '다시는 이런 문제 내지 말아야지' 하는 다짐을 유발하기도 했습니다.

위의 괄호 문자열 문제에서 현재 읽고 있는 위치(`|`)를 다음과 같이 열거형으로 표현할 수 있었듯이...

* 소괄호 안 (`( ... | ... )`)
* 대괄호 안 (`[ ... | ... ]`)

이 문제에서도 다음과 같은 열거형을 정의해 스택으로 관리할 수 있습니다. 설명의 편의상 이름을 붙이겠습니다.

* `S_IF`: `if`(-`else`)문의 첫 번째 본문 이전 (`if | ...` 혹은 `if | ... else ...`)
* `S_THEN`: `if`(-`else`)문의 첫 번째 본문 이후 (`if ... |` 혹은 `if ... | else ...)`
* `S_ELSE`: `if`-`else`의 두 번째 본문 이전 (`if ... else | ...`)
* `S_BLOCK`: 중괄호 안 (`{ ... | ... }`)

그런 다음에는 토큰을 읽을 때마다 다음과 같이 스택 갱신과 출력을 병행하면 됩니다. "스택의 가장 위에 토큰 X가 있을 때"는 글 작성의 편의상 "X 상태일 때"로 축약합니다.

* 용어 설명
	* **`X` 상태**: '스택의 가장 위에 토큰 `X`가 있을 때'를 글 작성의 편의상 축약한 형태입니다.
	* **본문이 올 자리**: `S_IF`나 `S_ELSE` 상태를 말합니다. 이 경우 다음 토큰으로는 본문을 구성할 수 있는 토큰(`;`, `{`, `if`)만 올 수 있습니다.
		* 특히 `S_BLOCK`은 *본문이 올 자리*가 **아닙니다**.
	* **`if`문 정리**: `S_THEN` 상태이고 다음 토큰이 `else`가 아닌 경우에는 해당 토큰이 `if`-`else`문이 아닌 `if`문에 해당하며, 이때 `else`문으로 전이하지 않는 `S_THEN`을 뽑는 것을 일컫습니다.
		* 처리해야 할 `S_THEN`이 여러 겹 있을 수 있음에 유의해야 합니다.
	* **본문 여닫기**: 본문이 열릴 때와 닫힐 때 필요한 특수한 처리를 일컫습니다. 중괄호가 생략됐는지 판단하여 추가로 출력하고, 본문이 닫힐 때 `S_IF`는 `S_THEN`으로 갱신, `S_ELSE`는 뽑아야 합니다.
		* *`if`문을 정리*하는 도중에도 *본문 닫기* 판정이 생길 수 있음에 유의해야 합니다.
* 세미콜론(`;`)
	* *본문이 올 자리*일 경우 *본문 여닫기*를 합니다.
	* 그렇지 않으면 세미콜론이 일반 구문이므로 *`if`문을 정리*합니다.
* 여는 중괄호(`{`)
	* 이 토큰은 본문의 첫 토큰으로만 등장할 수 있습니다. *본문 열기*를 하고 스택에 `S_BLOCK`을 넣습니다.
* 닫는 중괄호(`}`)
	* 이 토큰은 본문의 마지막 토큰으로만 등장할 수 있습니다. *`if`문을 정리*하고 스택의 `S_BLOCK`을 뽑은 뒤 *본문 닫기*를 합니다.
* `if`
	* *본문이 올 자리*일 경우 *본문 열기*를 한 뒤 스택에 `S_IF`를 넣습니다.
	* 그렇지 않으면 `if`(-`else`)문이 일반 구문이므로 *`if`문을 정리*하고 스택에 `S_IF`를 넣습니다.
* `else`
	* 이 토큰은 본문의 바로 다음에만 등장할 수 있습니다. 스택의 `S_THEN`을 `S_ELSE`로 갱신합니다.
* `end`
	* 입력의 종료를 나타내는 토큰입니다. *`if`문을 정리*합니다.

여기까지가 제가 의도한 대회 중에 작성할 수 있는 풀이였고, <AWp art="Dangling else" frag="Avoiding the conflict in LR parsers">모종의 방법</AWp>을 이용하면 문제에서 제시하는 언어(문법이 아닙니다!)<Fn id="grammar-vs-language" />를 LR 파싱할 수 있으니 관심이 있다면 도전해보시는 것도 좋겠습니다. 실제로 데이터 검증 코드가 이렇게 구현되어 있습니다.

#### 문제 여담 \{#cpc2024e1-trivia\}

사실 C에서는 `0`이 팔진수라는 것도 알고 계셨나요? C17 표준의 최종안인 <AOut href="https://files.lhmouse.com/standards/ISO%20C%20N2176.pdf#subsection.6.4.4">ISO C N2176</AOut>에서는 정수 상수의 문법이 다음과 같이 정의되어 있습니다. <sub>opt</sub>는 바로 앞의 문법 요소가 있어도 되고, 없어도 된다는 의미입니다.

* *integer-constant*:
	* *decimal-constant* *integer-suffix*<sub>opt</sub>
	* *octal-constant* *integer-suffix*<sub>opt</sub>
	* *hexadecimal-constant* *integer-suffix*<sub>opt</sub>
* *decimal-constant*:
	* *nonzero-digit*
	* *decimal-constant* *digit*
* *octal-constant*:
	* `0`
	* *octal-constant* *octal-digit*
* (중략)
* *nonzero-digit*: one of
	* `1` `2` `3` `4` `5` `6` `7` `8` `9`

십진 상수(*decimal-constant*)는 0으로 시작할 수 없고(*nonzero-digit*), 0으로 시작하는 것은 모두 팔진 상수(*octal-constant*)인 것으로 정의되어 있습니다. `0`도 예외는 아닙니다.

### <ABoj prob={32183} /> (F1번, 오픈 콘테스트 전용) \{#cpc2024f1\}

<Statement prob={32183} />

<Caution>
원본 문제의 다음 부분에 오타가 있습니다. 백준 온라인 저지가 아닌 블로그에 게시한 내용이 올바른 지문입니다.

<blockquote>
* `x+`는 `x`와 일치하는 비트열이 $$1$$개 이상 연결되어 있는 비트열을 찾는다. 즉, ~~`x`는~~ **`x+`는** `x|xx|xxx|...`와 같은 의미를 가진다.
</blockquote>
</Caution>

서비스 종료 이전 이 문제의 난이도는 <SolvedTier long tier="d4" />이며, 알고리즘 분류는 <SolvedTag tag="data_structures" /> <SolvedTag tag="string" /> <SolvedTag tag="segtree" /> <SolvedTag tag="lazyprop" /> <SolvedTag tag="regex" />이었습니다.

이 문제를 구상하는 데 여러 문제의 영향을 받았습니다.

* 원래 <ABoj prob={1013} />/<ABoj prob={2671} />의 지문을 그대로 차용한 쿼리 버전으로 기획했던 문제였습니다. 문제 출제 당시 기업에서 커뮤니티 문제를 표절하는 사건이 발생해 문제의 소지를 피하고자 현재의 지문과 정규식으로 변경했습니다.
* <ABoj prob={17407} />와 <ABoj prob={22854} />의 영향을 받았으며, 특히 이 문제를 후자의 (그나마) 쉬운/접근성 있는 버전으로 포지셔닝하려는 의도도 어느 정도 있었습니다.
	* '접근성 있는'은 후자를 풀 때 상수 커팅을 하는 게 힘들었던 것이 기억에 남아서 썼던 표현입니다. 맞은 사람 페이지를 보니까 다행히 상수 커팅까지 갈 필요는 없게 잘 세팅된 것 같네요.

이 문제를 해결하는 데는 여러 단계의 발상이 필요합니다.

#### 정규식을 DFA로 변환 \{#cpc2024f1-regex-to-dfa\}

이 문제를 해결하려면 정규식을 DFA로 변환하는 일련의 흐름을 사전 지식으로 알고 있어야 합니다.

##### 정규식 \{#cpc2024f1-regex\}

이 문제에서는 형식언어론에서 다루는, <AWp lang="ko" art="정규 언어"><Term term="regular language">정규 언어</Term></AWp>를 표현할 수 있는 <strong><AWp art="Regular expression" frag="Formal language theory">정규식</AWp></strong>을 다룹니다. 정규식에 문법 확장을 잘못 도입했다가는 표현력이 정규 언어를 뛰어넘어서 '정규'식이라고 할 수 없기 때문에 유의해야 합니다.

문제에서 정규식의 문법 일부를 설명했는데, 문제에는 없지만 상황에 따라 정규 언어의 표현력 이내에서 다음 연산자나 문법을 추가로 도입하기도 합니다.

* 아무 문자열도 찾지 않는 공집합 정규식. 관례상 공집합 기호를 차용해 `∅`으로 적습니다.
* 빈 문자열 `""`을 찾는 정규식. 관례상 `ε`(엡실론)으로 적습니다.
* 0개 혹은 1개를 찾는 연산자 `x?`. `ε|x`와 같은 의미를 가집니다.
* 집합에 속하는 한 문자를 찾는 문법 `[...]`. 예를 들어 `[abc]`는 `a|b|c`와 같은 의미를 가집니다.
	* 집합에 속하지 *않는* 한 문자를 찾는 문법 `[^...]`

참고로 관례상 `x*`는 '<Term term="Kleene star">클레이니 스타</Term>', `x+`는 '<Term term="Kleene plus">클레이니 플러스</Term>'라고 읽습니다.

##### 비결정론적 유한 상태 기계 \{#cpc2024f1-nfa\}

<strong><AWp lang="ko" art="비결정론적 유한 상태 기계"><Term term="nondeterministic finite automaton; NFA">비결정론적 유한 상태 기계</Term></AWp></strong>는 유한개의 상태를 가지는 이론적인 기계의 일종으로, 그대로 문자열 인식에 사용하기도 하고 아래에서 설명할 DFA로 변환하는 중간 단계로 사용하기도 합니다. NFA는 다음 규칙에 따라 문자열을 인식합니다.

* 기계에 있는 상태 중 정확히 하나가 초기 상태로 지정되어 있으며, 한편 하나 이상의 상태가 <Term term="accepting state">수용 상태</Term>로 지정되어 있습니다.
* 각 상태마다 특정한 문자를 입력받았을 때 전이할 수 있는 다음 상태의 집합이 정해져 있습니다.
	* 가능한 다음 상태는 0개, 1개, 혹은 여러 개일 수도 있습니다.
	* 예를 들어 1번 상태에서 `0`을 입력받으면 2번 혹은 3번 상태, `1`을 입력받으면 2번 혹은 4번 상태로 전이할 수 있도록 설정할 수 있습니다.
* 초기 상태에서 시작해 문자열의 모든 문자를 순서대로 입력하면서 보드게임을 하듯이 상태를 전이시킵니다.
	* 그 결과 상태 전이가 없는 상태에 갇히지 않고 수용 상태에서 멈추는 방법이 *하나라도* 있다면 기계는 이 문자열을 <strong><Term term="accept">인식</Term></strong>합니다.
	* 수용 상태에 멈추는 방법이 하나도 없다면 기계는 이 문자열을 <strong><Term term="reject">거부</Term></strong>합니다.

추가로 빈 문자열 `ε` 역시 문자처럼 취급해서 기계에 `ε` 전이가 있고 작동하는 도중 원하는 때에 원하는 횟수만큼 `ε` 전이를 할 수 있도록 확장하는 경우도 있고, 이 경우를 따로 ε-NFA라고도 합니다.

##### 결정론적 유한 상태 기계 \{#cpc2024f1-dfa\}

<strong><AWp lang="ko" art="결정론적 유한 상태 기계"><Term term="deterministic finite automaton; DFA">결정론적 유한 상태 기계</Term></AWp></strong> 역시 유한개의 상태를 가지는 기계인데, NFA와 달리 다음 두 가지 제한이 있어서 결정론적으로 동작하고 구현하기도 훨씬 쉽습니다('하나라도'를 신경쓰지 않아도 되므로).

* 각 상태-문자열 쌍마다 **최대** 하나의 상태로만 전이할 수 있습니다. 상태 전이가 없는 것은 가능합니다.
* `ε` 전이가 금지됩니다.

NFA보다 할 수 있는 것이 적어 보이지만, 놀랍게도 DFA와 NFA 모두 정규식/정규 언어와 **정확히 같은 표현력**을 가집니다.

문제에서 제시한 정규식을 DFA로 변환하려면 다음의 세 단계를 거치면 됩니다.

##### 톰슨 구성 \{#cpc2024f1-thompson-s-construction\}

<strong><AWp art="Thompson's construction"><Term term="Thompson's construction">톰슨 구성</Term></AWp></strong> 알고리즘으로 원하는 정규식을 ε-NFA로 변환할 수 있습니다. 정규식의 모양을 따라 재귀하기 때문에 위키백과 항목의 삽화만 봐도 직관적으로 이해할 수 있습니다.

톰슨 구성 알고리즘 이외에도 같은 변환을 수행하는 <AWp art="Glushkov's construction algorithm"><Term term="Glushkov's construction algorithm">글루쉬코프 구성 알고리즘</Term></AWp>이 있고 이쪽은 `ε` 전이를 생성하지 않지만, 톰슨 구성 알고리즘이 가장 널리 사용되고 있습니다.

##### 멱집합 구성 \{#cpc2024f1-powerset-construction\}

이렇게 얻은 NFA는 <strong><AWp art="Powerset construction"><Term term="powerset construction">멱집합 구성</Term></AWp></strong> 알고리즘으로 DFA로 변환할 수 있습니다. NFA의 모든 상태의 부분집합이 각각 DFA의 상태 *하나*가 되기 때문에 <Term term="subset construction">부분집합 구성</Term>이라고도 합니다.

##### DFA 최소화 \{#cpc2024f1-dfa-minimization\}

멱집합 구성으로 얻은 DFA는 상태의 개수가 불필요하게 많을 수 있는데, 여기서 초기 상태에서 갈 수 없는 상태와 수용 상태로 갈 수 없는 상태를 없애고 서로 구별할 수 없는 상태를 합쳐서 상태의 개수를 최소화하는 것을 <strong><AWp art="DFA minimization"><Term term="DFA minimization">DFA 최소화</Term></AWp></strong>라고 합니다.

위의 모든 과정을 직접 계산해야 하는 것은 아니고, 웹상의 도구 중 아무거나 하나를 골라잡으면 됩니다. 저는 <AOut href="https://cyberzhg.github.io/toolbox/min_dfa">CyberZHG님의 웹 앱</AOut>을 이용했습니다.

문제에서 제시한 정규식인 `(1(10)+1*|0+10)+`을 <AOut href="https://cyberzhg.github.io/toolbox/min_dfa?regex=KDEoMTApKzEqfDArMTApKw==">최소 DFA로 변환한 결과</AOut>는 다음과 같습니다. 초기 상태는 0이며, 모든 상태 번호에서 1을 빼는 가공을 거쳤습니다.

<Table.DFA client:load data={[
	{ id: 0, '0': 2, '1': 3 },
	{ id: 1, '0': 2, '1': 7, accept: true },
	{ id: 2, '0': 2, '1': 4 },
	{ id: 3, '1': 5 },
	{ id: 4, '0': 6 },
	{ id: 5, '0': 1 },
	{ id: 6, '0': 2, '1': 3, accept: true },
	{ id: 7, '0': 1, '1': 7, accept: true },
]} />

상태 전이표의 ❌ 표시는 상태 전이가 없어서 DFA가 문자열을 거부하는 경우이며, 코드로 구현할 때는 `-1`을 대신 사용할 수 있습니다.

#### 상태 전이를 세그먼트 트리로 관리 \{#cpc2024f1-transition-in-segtree\}

함수라고 하면 정의역의 모든 원소를 공역의 원소 하나씩과 대응시킨 것인데, 위 상태 전이 함수의 경우에는 정의역의 원소가 8개뿐이므로 배열로 전부 기록할 수 있습니다. 예를 들어 비트열 `0`에 해당하는 상태 전이 배열은 C/C++ 문법으로 `{ 2, 2, 2, -1, 6, 1, 2, 1 }`입니다. 추가로 길이가 2 이상인 비트열의 상태 전이 배열 역시 단순 함수 합성으로 구할 수 있습니다.

이제 상태 전이 배열에 결합법칙이 성립하는 이항 연산(함수 합성)이 있으니 문자열 전체의 상태 전이 배열을 세그먼트 트리로 관리할 수 있습니다. 문자열 인식은 문자열의 상태 전이 배열을 구하고 초기 상태 0에서의 함숫값, 즉 `[0]`번째 원소를 구한 뒤 그 상태가 수용 상태인지 확인하면 됩니다.

#### 갱신 결과를 세그먼트 트리에 미리 저장 \{#cpc2024f1-precalculated-update\}

구간 갱신까지 처리하려면 추가로 한 가지 발상이 더 필요합니다. 이 문제에서는 함수 합성을 고려하더라도 가능한 갱신이 `00`, `01`, `10`, `11`의 네 종류뿐인데, 상태 전이 함수의 모든 함숫값을 같이 저장했으니 **가능한 모든 갱신 결과도 같이 저장하지 못할 이유가 없다**는 것입니다.

구체적으로는 각 비트를 다음과 같이 저장하고...

* `0` 비트: `{ ("0"의 상태 전이 배열), ("0"의 상태 전이 배열), ("1"의 상태 전이 배열), ("1"의 상태 전이 배열) }`
* `1` 비트: `{ ("0"의 상태 전이 배열), ("1"의 상태 전이 배열), ("0"의 상태 전이 배열), ("1"의 상태 전이 배열) }`

구간 갱신은 다음과 같이 처리하면 됩니다.

* `00` 질의: `{ a, b, c, d }`를 `{ a, a, d, d }`로 갱신
* `01` 질의: `{ a, b, c, d }`를 `{ a, b, c, d }`로 갱신
* `10` 질의: `{ a, b, c, d }`를 `{ a, c, b, d }`로 갱신
* `11` 질의: `{ a, b, c, d }`를 `{ a, d, a, d }`로 갱신

문자열 인식은 `01` 질의에 해당하는 `b`를 꺼내어 위와 같이 확인하면 됩니다.

#### 문제 여담 \{#cpc2024f1-trivia\}

<Image src={cpc2024f1_aejuga} alt="빈 배경 앞에 쉰 에스테베즈가 서 있는 저화질 밈 이미지. 이미지에는 '그래요 저는 애주가예요'와 함께 '형식언어론 같이 덕질할 사람이 없어'를 애주가의 삼행시인 것처럼 적은 문구가 있다." />

*원본 이미지의 출처는 X의 <AOut href="https://twitter.com/NewGenHmmm/status/1115834651908759553">신세대 의문(@NewGenHmmm)</AOut>님입니다.*

## CPC 2026 \{#cpc2026\}

<AOut href="https://www.acmicpc.net/category/detail/4694" unavailable>2026 중앙대학교 프로그래밍 경진대회</AOut>에 두 문제를 출제하고, 세 문제에 삽화를 제공했습니다.

<AOut href="https://upload.acmicpc.net/e84bc5f4-d952-444b-a89a-21bca7b32e1b/">공식 에디토리얼</AOut>이 공개되어 있습니다.

이번 대회는 <ABoj prob={35453}>A1번</ABoj>을 제외한 모든 문제가 지금까지 ChAOS에서 개최한 대회의 기출문제 패러디로 구성되어 있다는 특이사항이 있습니다. 대회에 출제진으로 참여하기 이전부터 미리 문제를 구상하고 있었는데, 다행히 대회의 컨셉에 맞게 문제를 끼워맞출 수 있었습니다.

### <ABoj prob={35462} /> (D3번) \{#cpc2026d3\}

<Statement prob={35462} />

서비스 종료 이전 이 문제의 난이도는 <SolvedTier long tier="g1" />이며, 알고리즘 분류는 <SolvedTag tag="implementation" /> <SolvedTag tag="graphs" /> <SolvedTag tag="dag" /> <SolvedTag tag="topological_sorting" />이었습니다.

당연히 <AOut href="https://play.google.com/store/apps/details?id=com.ecffri.arrows">오만 곳에서 광고를 하고 있는 이 게임</AOut>이 소재입니다(실제로 플레이해본 적은 없습니다). 광고를 처음 봤을 때부터 '이거 위상 정렬로 풀 수 있는 거 아닌가?' 하는 생각을 항상 했고, 이 문제도 위상 정렬로 풀 수 있습니다. 각 화살표마다 정점을 하나씩 만들고, $$i$$번 화살표의 이동 경로에 $$j$$번 화살표가 있을 경우 $$i$$에서 $$j$$로 가는 유향 간선을 추가하면 됩니다. 자기 자신과의 충돌은 특수하게 확인해야 합니다.

문제를 처음 세팅했을 때는 예상하지 못했던 점이 *충돌 판정*이 생각보다 어렵다는 점이었습니다. 저는 $$N \times M$$짜리 배열 위에 지나가는 화살표의 번호를 기록해서 다른 화살표와의 충돌을 판정했고, 자기 자신과의 충돌 판정은 추가로 같은 배열 위에 초기 배치를 기준으로 시작점에서 떨어진 거리를 추가로 기록해서 해결했습니다.

### <ABoj prob={35465} /> (E3번) \{#cpc2026e3\}

<Statement prob={35465} />

<Caution>
'워'가 하이퍼링크에서 빠진 것은 버그가 아니라 기능입니다.
</Caution>

서비스 종료 이전 이 문제의 난이도는 <SolvedTier long tier="p3" />이며, 알고리즘 분류는 <SolvedTag tag="math" /> <SolvedTag tag="dp" /> <SolvedTag tag="bitmask" /> <SolvedTag tag="dp_bitfield" /> <SolvedTag tag="exponentiation_by_squaring" /> <SolvedTag tag="lis" />였습니다.

<ABoj prob={30799} />을 푸는 중 쓸데없이 복잡한 잘못된 풀이를 하나 떠올렸는데, 그 풀이가 정해인 문제가 있으면 재밌겠다는 생각이 들어 만든 문제입니다. 사실 CPC 2026에 출제진으로 참여하기로 한 이유 중 절반 정도가 이 문제라고 생각합니다.

문제의 설명을 보면 [<Term term="longest increasing subsequence; LIS">가장 긴 증가 부분 수열</Term>](https://eatchangmyeong.github.io/2022/01/20/why-is-lis-algorithm-so-confusing.html)을 구하는 문제처럼 보이고, 실제로도 LIS 알고리즘을 응용하는 문제가 맞습니다. (설명의 편의를 위해 0번 칸과 $$N + 1$$번 칸을 무시하겠습니다.) 다만 실제로는 모든 경우의 수를 요구하기 때문에 LIS 알고리즘을 그대로 사용할 수는 없습니다. 추가로 $$N$$의 최댓값이 10억인데 일단 $$N$$이 작다고 가정하고 '나이브' DP로 문제를 풀어 보겠습니다.

LIS 알고리즘에서는 지금까지 입력받은 수열의 정보를 배열로 관리하는데, 각 칸의 높이는 1부터 7까지만 가능하기 때문에 이 배열로 가능한 경우의 수는 $$2^7 = 128$$가지이고, 비트마스킹을 통해 0 이상 127 이하의 정수로 표현할 수 있습니다. 각 LIS 배열에 대해 가능한 경우의 수를 나타내는 128칸짜리 DP 배열을 만든 뒤, LIS 알고리즘을 통해 점화식을 만들어서 다음 항을 구하는 것을 $$N$$번 반복하면 됩니다. 여기까지는 $$O(N)$$입니다.

그런데 잘 생각해보면 DP 배열의 점화식은 선형성을 가지므로 128×128 행렬로 표현할 수 있습니다. 그리고 행렬의 거듭제곱은 $$O(\log N)$$으로 구할 수 있습니다! 시간 복잡도에서 세제곱 항을 생략했는데, $$128^3$$은 약 2백만이므로 실질적으로는 문제가 없습니다. 제가 짠 정해는 왜인지 꽤 비효율적으로 작동해서 452 ms인데, *그래도* 2초 안에는 충분히 돌아갑니다.

마지막으로 문제에서 요구하고 있는 것은 특정한 LIS 배열이 아니라 *케이크의 개수*이므로 `__builtin_popcount`를 사용해 LIS 배열을 LIS의 길이로 환산한 뒤 일치하는 것을 합해서 출력하면 됩니다.

구하는 실험실이 하나도 없을 때 `The cake is a hing`을 출력하는 조건은 원본 문제에서 출력하는 `Hing`의 패러디이면서 문제의 난이도를 조금 더 올리는 역할을 했습니다. 이쪽은 간단한 조건을 확인해서 판정할 수 있는데, 둘 중 하나를 만족하면 됩니다.

* $$N \ge 1$$이고 $$W = 1$$일 때 (비어 있지 않은 수열의 LIS를 구했는데 길이가 0인 경우에 해당합니다.)
* $$N < W + 1$$일 때 (LIS의 길이가 수열의 길이보다 긴 경우에 해당합니다.)

참고로 $$10^9 + 7$$로 나눈 나머지가 0인지 판정하면 **<WA>틀립니다</WA>**! 특별히 이 경우를 저격하는 테스트 케이스를 찾아서 넣어 두었습니다.

## 여담 \{#trivia\}

물론 문제 출제에서도 보람을 느끼고 있지만, 삽화 작업에서도 꽤 보람을 느끼는 편입니다. 일단 삽화는 눈에 바로 보이는 편이니까요.

위에 언급된 대회의 삽화 중에 뭔가 은근히 귀엽고 나눔스퀘어라운드(이 블로그의 제목체입니다)가 보이면 제 작업물이라고 생각해 주세요. 특히 CHAC 2022와 CPC 2024의 모든 삽화는 제가 그렸습니다. 😜

<Image src={x1829849604579606873} alt="잇창명의 2024년 8월 31일 X 게시물: '사실은 이번 대회 준비하면서 공을 많이 들인 3짤4짤을 보여드리고 싶었는데요'. CPC 2024의 '울타리 공사' 첫 번째 삽화, '컵 쌓기' 세 번째 삽화, '통신 시스템의 성능 저하' 첫 번째 삽화와 두 번째 삽화가 첨부되어 있다. 역시 잇창명의 2023년 10월 16일 게시물 '오랜만에 생각났으니까 올려봐야지 제가 대학교 프로그래밍 대회 운영진으로 참가하면서 그렸던 귀여운 삽화를 봐주세요.'를 인용하였다." />